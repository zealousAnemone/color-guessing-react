import React from "react";

const Controls = () => {
  return (
    <div>
      <button>New Colors</button>
      <button>Easy</button>
      <button>Hard</button>
    </div>
  );
};

export default Controls;
